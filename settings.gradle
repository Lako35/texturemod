pluginManagement {
    repositories {
        // Fabric publishes Loom here
        maven { url = 'https://maven.fabricmc.net/' }
        // Usuals
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "archistructurehelper-client"
